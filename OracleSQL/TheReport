--Score:20|Diff:Med

SELECT
CASE 
    WHEN STU_GRADE>7 THEN STU_NAME
END AS STU_NAME, STU_GRADE, STU_MARK 
FROM (SELECT S.NAME STU_NAME, (SELECT G.GRADE FROM GRADES G WHERE S.MARKS BETWEEN MIN_MARK AND MAX_MARK) STU_GRADE,S.MARKS STU_MARK 
      FROM STUDENTS S)
ORDER BY STU_GRADE DESC,STU_NAME;
